export default {
  setUserInfo (state, data) {
    state.userInfo = data;
  },
  setShopInfo (state, data) {
    state.shopInfo = data;
  },
  setRedirectUrl (state, data) {
    state.redirectUrl = data
  }
}
